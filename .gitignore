/* File: .gitignore */

# macOS system files (prevents annoying invisible files from syncing)
.DS_Store

# Node/NPM dependencies (the massive folder of downloaded code)
node_modules/

# Eleventy build output (the compiled website)
_site/